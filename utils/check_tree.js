let alphabet = 'abcdefghijklmnopqrstuvwxyz'.split('');
//console.log(alphabet);
let wordmap=s
let L=LW
for  (let i = 0; i < LW.length; i++) {
    L[i]]LW[i].strip('\n');
}
let cache = this.cache.text;
let wordgraph= cache.get('wordgraph');
let LW= cache.get('legal_words').split('\n');
let sortedDict = sorted(LW, {
  "key": len
});
function check_tree_len(word1, word2) {
    let tree1 = [[sortedDict.indexOf(word1), 0, 0]];
    let tree2 = [[sortedDict.indexOf(word2), 0, 0]];

    if (word1 === word2) {
	return 0;
    }

    let nsteps1 = 1;
    let nsteps2 = 1;
    let fails = 0;
    while !(tree1.some(r=> tree2.includes(r))){
	if (fails === 3) {
	    return -1;
	}
	if tree1.length.
    }
}
